# docker-compose Variablen — kopiere zu .env und passe an.

# Host-Pfade die in den Container gemountet werden
MUSIC_DIR=/home/johncena/Musik
CONFIG_DIR=./config
CACHE_DIR=./cache

# UID/GID des Host-Users (id -u / id -g)
APP_UID=1000
APP_GID=1000
